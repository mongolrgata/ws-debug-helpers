- сейчас selectControlGUI позволяет выбирать только те контролы, которые присутствуют в ControlStorage, но туда иногда попадают не все контролы (не времени объяснять, но это так). Возможно, стоит дополнительно пробегаться по потомкам/родителям контролов
- разработать «функцию-дополнитель» (название временное)
- переопределить методы store и remove у $ws.single.ControlStorage «функцией-дополнитель» (название временное), дополнительно сохраняя контролы в структуру поддерживающую повторяющиеся ключи
- сделать уже Array.prototype.toConsole
- перестал работать вывод имени класса контрола в консоли (берётся имя функции-конструктора, а не результат вызова метода toString)